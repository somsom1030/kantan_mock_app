import 'package:flutter/material.dart';

class TrashFragment extends StatelessWidget {
  const TrashFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("ccccc"),
      color: Colors.green,
    );
  }
}
