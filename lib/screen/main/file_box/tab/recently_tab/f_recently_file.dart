import 'package:flutter/material.dart';

class RecentlyFragment extends StatelessWidget {
  const RecentlyFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("bbbbbb"),
      color: Colors.red,
    );
  }
}
